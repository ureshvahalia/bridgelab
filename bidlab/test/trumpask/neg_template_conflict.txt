## Attaching a template onto a node that already has an explicit child
## under the name a template entry would produce -- must be fatal.
$.1S.3S@.4N.   := (Points>=12) :? RKCB;
$.1S.3S@.4N.5C. := (Points>=0);

$.?.RKCB.5C. := (Trumpkcs ?= 1) OR (Trumpkcs ?= 4);
$.?.RKCB.5D. := (Trumpkcs ?= 0) OR (Trumpkcs ?= 3);
END
